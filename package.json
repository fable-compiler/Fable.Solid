{
    "private": "true",
    "scripts": {
        "postinstall": "dotnet tool restore",
        "start": "dotnet fable watch ./src/App --outDir ./_watch --extension .jsx --run vite",
        "build": "dotnet fable ./src/App --outDir ./_watch --extension .jsx --run npx vite build --outDir ./docs",
        "publish": "dotnet fsi build.fsx publish"
    },
    "dependencies": {
        "@solidjs/router": "^0.13.5",
        "solid-js": "^1.8.17"
    },
    "devDependencies": {
        "vite": "^5.2.12",
        "vite-plugin-solid": "^2.10.2"
    }
}
